<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <title>Entryforex Admin</title>

    <link rel="stylesheet" href="/bower_components/bootstrap/dist/css/bootstrap.min.css"></link>
</head>
<body>

<nav class="navbar navbar-default" role="navigation">
    <div class="container">
        <a class="navbar-brand" href="/">Entryforex Admin</a>

        <form class="navbar-form navbar-right" action="/logout">
            <button type="submit" class="btn btn-default">Logout</button>
        </form>
    </div>
</nav>

<div class="container">
    <div class="jumbotron">
        <h1>Total PnL: <%=totalPnL%></h1>
    </div>
    <table class="table table-bordered table-hover">
        <tr>
            <th>User</th>
            <th>Instrument</th>
            <th>Amount</th>
            <th>Open price</th>
            <th>Open time</th>
        </tr>
        <%openDeals.forEach(function(deal) {%>
            <tr>
                <td><%=deal.name%> &lt;<%=deal.email%>&gt;</td>
                <td><%=deal.instrument%></td>
                <td><%=parseFloat(deal.amount)%></td>
                <td><%=parseFloat(deal.price)%></td>
                <td><%=deal.open_time%></td>
            </tr>
        <%})%>
    </table>
</div>

<script src="/bower_components/jquery/dist/jquery.min.js"></script>
<script src="/bower_components/bootstrap/dist/js/bootstrap.min.js"></script>

</body>
</html>
